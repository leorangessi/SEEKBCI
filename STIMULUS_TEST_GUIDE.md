# 刺激程序改进测试指南

## 测试环境准备
1. 清除浏览器缓存（Ctrl+Shift+Delete 或 Ctrl+F5）
2. 打开 `web_frontend/index.html`
3. 准备测试项目

## 测试一：Python代码验证

### 测试1.1：缺少import语句
**步骤**：
1. 进入编辑器，创建新项目
2. 添加一个对象（如按钮）
3. 设置动作类型为"Python动作"
4. 在Python代码中输入：
   ```python
   result = np.array([1, 2, 3])
   print(result.mean())
   ```
5. 保存项目
6. 返回项目管理器，点击"运行"

**预期结果**：
- ❌ 显示错误提示
- 错误信息：`对象 "xxx" 的Python脚本缺少包引入`
- 详细信息：`缺少包: numpy`
- 建议：`请在画布的Python代码编辑器中添加: import numpy`

### 测试1.2：语法错误（缺少冒号）
**步骤**：
1. 在Python代码中输入：
   ```python
   def test_function()
       print("hello")
   ```
2. 保存并运行

**预期结果**：
- ❌ 显示错误提示
- 错误信息：`对象 "xxx" 的Python代码存在语法错误`
- 详细信息：`第 1 行缺少冒号`

### 测试1.3：括号不匹配
**步骤**：
1. 在Python代码中输入：
   ```python
   result = calculate(1, 2, 3
   print(result)
   ```
2. 保存并运行

**预期结果**：
- ❌ 显示错误提示
- 错误信息：`对象 "xxx" 的Python代码存在语法错误`
- 详细信息：`第 1 行括号不匹配`

### 测试1.4：正确的Python代码
**步骤**：
1. 在Python代码中输入：
   ```python
   import numpy as np
   result = np.array([1, 2, 3])
   print(result.mean())
   ```
2. 保存并运行

**预期结果**：
- ✅ 验证通过，刺激程序正常启动

## 测试二：频率冲突检测

### 测试2.1：同一画布内频率冲突
**步骤**：
1. 在编辑器中创建两个对象
2. 设置对象1频率为 10.0 Hz
3. 设置对象2频率为 10.0 Hz（相同）
4. 保存项目
5. 返回项目管理器，点击"运行"

**预期结果**：
- ❌ 显示错误提示
- 错误信息：`画布 "Page 1" 存在重复频率`
- 详细信息：`对象 "对象1" 和 "对象2" 都使用 10.0 Hz`
- 建议：`同一画布内每个对象必须使用不同的频率以确保识别准确性`
- 标记画布：`画布: Page 1`

### 测试2.2：不同画布相同频率（应该允许）
**步骤**：
1. 在Page 1创建对象，频率10.0 Hz
2. 添加新页面Page 2
3. 在Page 2创建对象，频率10.0 Hz
4. 保存并运行

**预期结果**：
- ✅ 验证通过（不同画布可以使用相同频率）

### 测试2.3：同一画布不同频率
**步骤**：
1. 在同一画布创建多个对象
2. 设置不同频率：8.0, 9.0, 10.0, 11.0 Hz
3. 保存并运行

**预期结果**：
- ✅ 验证通过，刺激程序正常启动

## 测试三：刺激控制窗口

### 测试3.1：浮动图标
**步骤**：
1. 运行一个有效的项目
2. 刺激程序启动后，观察右上角控制面板
3. 点击面板标题右侧的"−"按钮

**预期结果**：
- 控制面板消失
- 右上角出现蓝色圆形浮动按钮（🎮图标）
- 按钮大小：60x60px
- 悬停时按钮放大并增强阴影

### 测试3.2：展开面板
**步骤**：
1. 点击浮动的🎮图标

**预期结果**：
- 浮动按钮消失
- 控制面板重新显示
- 所有控制选项正常显示

### 测试3.3：置于顶层
**步骤**：
1. 展开控制面板
2. 观察"置于顶层"按钮（默认应显示"✓ 置于顶层"）
3. 点击该按钮切换状态

**预期结果**：
- 第一次点击：按钮文字变为"置于顶层"（无✓）
- 再次点击：按钮文字变为"✓ 置于顶层"
- 面板的z-index在1000和10000之间切换

### 测试3.4：背景透明度
**步骤**：
1. 观察"背景透明度"滑块（默认100%）
2. 拖动滑块到50%
3. 观察背景变化
4. 拖动到0%

**预期结果**：
- 滑块实时响应
- 百分比数值实时更新
- 背景透明度实时变化
- 0%时背景完全透明
- 100%时背景完全不透明（黑色）
- 刺激对象不受影响，保持闪烁

### 测试3.5：关闭运行
**步骤**：
1. 点击"开始刺激"
2. 等待刺激开始运行
3. 点击最下方的"✕ 关闭运行"按钮

**预期结果**：
- 弹出确认对话框："刺激正在运行，确定要关闭吗？"
- 点击"确定"后，刺激停止
- 窗口关闭或返回编辑器

### 测试3.6：控制面板布局
**步骤**：
1. 展开控制面板
2. 检查所有元素的排列顺序

**预期结果**：
```
🎮 刺激控制                    [−]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
状态: 准备就绪
运行时间: 00:00
方块数量: X
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[▶️ 开始刺激]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✓ 置于顶层]
背景透明度: [====] 100%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[🖥️ 全屏模式]
[📊 显示/隐藏FPS]
[← 返回编辑器]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✕ 关闭运行]
```

## 测试四：倒计时显示

### 测试4.1：刺激程序倒计时
**步骤**：
1. 在编辑器中创建3-4个对象
2. 保存并运行项目
3. 点击"开始刺激"
4. 观察倒计时过程

**预期结果**：
- 屏幕中央显示大号数字：3...2...1...
- 倒计时数字颜色：蓝色（#00D9FF）
- 倒计时数字有脉冲动画效果
- **重点**：倒计时期间，所有对象显示为中等灰度（#888）
- **重点**：倒计时期间，对象不闪烁
- 倒计时结束后，对象开始正常闪烁

### 测试4.2：测试程序倒计时
**步骤**：
1. 打开 `test-stimulus.html`
2. 调整参数（频率、大小等）
3. 点击"开始测试"
4. 观察倒计时过程

**预期结果**：
- 屏幕中央显示倒计时：3...2...1...
- 倒计时期间，测试方块显示为中等灰度
- 倒计时期间，方块不闪烁
- 倒计时结束后，方块开始闪烁

### 测试4.3：倒计时样式
**步骤**：
1. 在倒计时期间仔细观察

**预期结果**：
- 字体大小：120px（非常大）
- 颜色：#00D9FF（主题蓝色）
- 位置：屏幕正中央
- 动画：每个数字有放大缩小效果
- 阴影：数字周围有发光效果
- 每个数字显示1秒

## 测试五：综合测试

### 测试5.1：完整工作流程
**步骤**：
1. 创建新项目，添加4个对象
2. 设置不同频率：8.0, 10.0, 12.0, 14.0 Hz
3. 为其中一个对象添加Python动作（正确的代码）
4. 保存项目
5. 运行项目
6. 观察倒计时
7. 刺激开始后，最小化控制面板
8. 调整背景透明度
9. 切换置顶选项
10. 停止刺激
11. 关闭运行

**预期结果**：
- 所有功能正常工作
- 无错误提示
- 界面流畅

### 测试5.2：错误处理
**步骤**：
1. 创建项目，故意制造多个错误：
   - 对象1：Python代码缺少import
   - 对象2和对象3：使用相同频率
   - 对象4：频率超出范围（25 Hz）
2. 运行项目

**预期结果**：
- 显示错误列表，包含所有错误
- 每个错误都有清晰的描述
- 标注了所在画布
- 提供了修复建议
- 无法运行项目（因为有错误）

### 测试5.3：警告处理
**步骤**：
1. 创建项目，制造警告：
   - 添加9个对象（超过推荐的8个）
   - 设置一个对象频率为5 Hz（低于推荐范围）
2. 运行项目

**预期结果**：
- 显示警告列表
- 警告用黄色标识
- 提示"存在警告，但仍可运行"
- 可以选择"继续运行"或"关闭"

## 测试六：浏览器兼容性

### 测试6.1：Chrome
- 测试所有功能
- 检查控制面板样式
- 检查浮动按钮动画

### 测试6.2：Firefox
- 测试所有功能
- 检查滑块样式（Firefox使用不同的滑块样式）

### 测试6.3：Edge
- 测试所有功能
- 检查backdrop-filter效果

## 常见问题排查

### 问题1：控制面板不显示
**解决方案**：
- 按H键切换显示
- 检查浏览器控制台是否有错误
- 清除缓存后重试

### 问题2：浮动按钮不出现
**解决方案**：
- 检查是否点击了最小化按钮
- 检查CSS是否正确加载
- 查看元素的display属性

### 问题3：倒计时期间对象仍在闪烁
**解决方案**：
- 检查 `showCountdown()` 函数是否正确设置了backgroundColor
- 确认代码已更新并清除缓存

### 问题4：Python验证不工作
**解决方案**：
- 检查 `validatePythonCode()` 函数是否存在
- 查看浏览器控制台的错误信息
- 确认项目数据结构正确

### 问题5：透明度不生效
**解决方案**：
- 检查 `updateOpacity()` 函数
- 确认使用的是rgba颜色格式
- 检查stimulus-container的样式

## 性能测试

### 测试7.1：多对象性能
**步骤**：
1. 创建8个对象
2. 运行刺激
3. 观察FPS

**预期结果**：
- FPS保持在55-60
- 无明显卡顿
- 控制面板响应流畅

### 测试7.2：长时间运行
**步骤**：
1. 运行刺激程序
2. 保持运行5分钟
3. 观察性能变化

**预期结果**：
- FPS稳定
- 内存使用正常
- 无内存泄漏

## 测试完成检查清单

- [ ] Python代码验证（缺少import）
- [ ] Python代码验证（语法错误）
- [ ] 频率冲突检测（同一画布）
- [ ] 频率冲突检测（不同画布）
- [ ] 浮动图标显示
- [ ] 控制面板展开/收起
- [ ] 置于顶层功能
- [ ] 背景透明度调节
- [ ] 关闭运行功能
- [ ] 刺激程序倒计时
- [ ] 测试程序倒计时
- [ ] 倒计时期间对象显示
- [ ] 综合工作流程
- [ ] 错误处理
- [ ] 警告处理
- [ ] 浏览器兼容性

## 报告问题

如果发现问题，请记录：
1. 问题描述
2. 重现步骤
3. 预期结果 vs 实际结果
4. 浏览器版本
5. 控制台错误信息（如有）
6. 截图（如适用）
