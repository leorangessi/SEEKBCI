# 刺激程序改进文档

## 改进日期
2026年2月23日

## 改进内容

### 1. 项目运行验证增强

#### 1.1 Python代码编译检查
- **功能**：对每个Python动作对象进行独立的代码验证
- **检查项**：
  - 包引入检测：自动检测使用的库（numpy, scipy, pandas, matplotlib, cv2, torch, tensorflow等）
  - 缺失import提示：如果代码中使用了库但没有import语句，会报错并提供修复建议
  - 语法错误检查：
    - 缺少冒号（def, if, for, while等语句）
    - 括号不匹配
    - 引号不匹配
    - 缩进不规范（建议4空格）
- **错误提示**：
  - 明确标注是哪个对象的Python脚本出错
  - 提供具体的错误行号和代码片段
  - 给出修复建议（如"请在画布中体现引入包的选项"）

#### 1.2 画布内频率冲突检测
- **功能**：检测同一画布内是否有重复的闪烁频率
- **检查范围**：仅检查同一画布，不同画布不检查
- **错误提示**：
  - 明确标注是哪张画布存在问题
  - 指出哪两个对象使用了相同频率
  - 提供修复建议："同一画布内每个对象必须使用不同的频率以确保识别准确性"

#### 1.3 验证错误显示
```javascript
// 错误信息结构
{
    type: 'error',  // 或 'warning', 'info'
    message: '对象 "按钮1" 的Python脚本缺少包引入',
    details: '画布: Page 1，缺少包: numpy, pandas',
    suggestion: '请在画布的Python代码编辑器中添加: import numpy, pandas',
    code: '# 建议添加:\nimport numpy\nimport pandas',
    page: 'Page 1'  // 标记画布名称
}
```

### 2. 刺激控制窗口优化

#### 2.1 浮动图标模式
- **功能**：控制面板可以最小化为浮动图标
- **图标样式**：
  - 圆形按钮，60x60px
  - 蓝色背景（#00D9FF）
  - 显示🎮图标
  - 悬停时放大并增强阴影
  - 固定在右上角
- **交互**：点击图标展开为完整控制面板

#### 2.2 置于顶层选项
- **功能**：控制刺激窗口是否始终在所有应用之上
- **默认状态**：开启（✓ 置于顶层）
- **实现**：
  - 开启时：z-index: 10000
  - 关闭时：z-index: 1000
- **按钮位置**：控制面板中部，分隔线之后

#### 2.3 背景透明度控制
- **功能**：调整刺激背景的透明度
- **默认值**：100%（完全不透明）
- **范围**：0-100%
- **实现**：
  ```javascript
  container.style.backgroundColor = `rgba(0, 0, 0, ${opacity / 100})`;
  ```
- **UI组件**：滑块控制器，实时显示百分比

#### 2.4 关闭运行按钮
- **功能**：完全关闭刺激程序
- **位置**：控制面板最下方，红色按钮
- **行为**：
  - 如果刺激正在运行，先确认是否关闭
  - 尝试关闭窗口（window.close()）
  - 如果无法关闭，返回编辑器

#### 2.5 控制面板布局
```
🎮 刺激控制                    [−]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
状态: 运行中
运行时间: 00:00
方块数量: 4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[▶️ 开始刺激]
[⏹️ 停止刺激]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✓ 置于顶层]
背景透明度: [====] 100%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[🖥️ 全屏模式]
[📊 显示/隐藏FPS]
[← 返回编辑器]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✕ 关闭运行]
```

### 3. 倒计时显示优化

#### 3.1 对象预显示
- **功能**：在倒计时3-2-1期间，显示所有对象但不闪烁
- **实现**：
  ```javascript
  // 倒计时期间设置所有对象为中等灰度
  stimulusBlocks.forEach(block => {
      block.element.style.backgroundColor = '#888';
      block.element.style.display = 'flex';
  });
  ```
- **目的**：让用户提前看到对象位置，做好准备

#### 3.2 倒计时样式
- **字体大小**：120px
- **颜色**：#00D9FF（主题蓝色）
- **位置**：屏幕正中央
- **动画**：脉冲效果（放大缩小）
- **阴影**：发光效果

#### 3.3 应用范围
- 刺激程序（stimulus.html）
- 测试程序（test-stimulus.html）

## 技术实现

### 文件修改清单
1. `web_frontend/project-manager.js`
   - 增强 `validateProject()` 函数
   - 新增 `validatePythonCode()` 函数
   - 改进错误提示显示

2. `web_frontend/stimulus.html`
   - 添加浮动控制按钮
   - 重构控制面板布局
   - 添加透明度滑块
   - 添加置顶和关闭按钮

3. `web_frontend/stimulus.js`
   - 添加控制面板状态变量
   - 实现 `toggleControlPanel()` 最小化功能
   - 实现 `toggleAlwaysOnTop()` 置顶功能
   - 实现 `updateOpacity()` 透明度控制
   - 实现 `closeStimulus()` 关闭功能
   - 改进 `showCountdown()` 显示对象

4. `web_frontend/test-stimulus.js`
   - 改进 `startTest()` 添加倒计时
   - 改进 `showCountdown()` 显示对象

## 使用说明

### 项目运行验证
1. 在项目管理器中点击"运行"按钮
2. 系统自动进行多层验证：
   - Python代码检查
   - 频率冲突检测
   - 对象数量警告
   - 频率范围检查
3. 如果有错误，显示详细的错误列表
4. 错误信息包含：
   - 错误类型（错误/警告/提示）
   - 具体问题描述
   - 所在画布名称
   - 修复建议
   - 相关代码片段

### 刺激控制窗口
1. **最小化**：点击面板右上角的"−"按钮
2. **展开**：点击浮动的🎮图标
3. **置顶**：点击"置于顶层"按钮切换
4. **透明度**：拖动滑块调整背景透明度
5. **关闭**：点击最下方的"✕ 关闭运行"按钮

### 倒计时功能
1. 点击"开始刺激"或"开始测试"
2. 屏幕显示倒计时：3...2...1...
3. 倒计时期间，所有对象以中等灰度显示
4. 倒计时结束后，开始正常闪烁

## 测试建议

### 验证功能测试
1. 创建包含Python动作的对象
2. 故意不添加import语句
3. 点击运行，验证是否提示缺少包引入
4. 在同一画布添加两个相同频率的对象
5. 点击运行，验证是否提示频率冲突

### 控制窗口测试
1. 启动刺激程序
2. 点击最小化按钮，验证是否变为浮动图标
3. 点击浮动图标，验证是否展开面板
4. 切换置顶选项，验证z-index变化
5. 调整透明度滑块，验证背景透明度变化
6. 点击关闭运行，验证是否正确关闭

### 倒计时测试
1. 在编辑器中创建多个对象
2. 点击运行刺激
3. 观察倒计时期间对象是否显示为灰色
4. 验证倒计时结束后对象开始闪烁

## 注意事项

1. **Python验证限制**：
   - 仅进行静态代码分析，无法检测运行时错误
   - 建议用户在实际运行前测试Python代码

2. **频率冲突**：
   - 仅检查同一画布内的冲突
   - 不同画布可以使用相同频率

3. **浏览器限制**：
   - `window.close()` 可能在某些浏览器中被阻止
   - 如果无法关闭窗口，会自动返回编辑器

4. **透明度效果**：
   - 透明度仅影响背景，不影响刺激对象
   - 建议在深色桌面环境下使用透明效果

## 后续优化建议

1. **Python代码编辑器**：在画布中添加专门的Python代码编辑器，支持语法高亮和自动补全
2. **频率建议**：根据对象数量自动推荐合适的频率组合
3. **实时验证**：在编辑器中实时显示验证结果，而不是等到运行时
4. **窗口拖拽**：允许拖拽控制面板到任意位置
5. **快捷键**：为控制面板功能添加键盘快捷键
